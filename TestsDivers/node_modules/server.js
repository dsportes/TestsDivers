'use strict';

class Server {
	constructor(url){
		this.url = url;
	}
	static option(){
		return "toto";
	}
	print(elt){
		// console.log(Server.option() + this.url);
		elt.innerHTML = Server.option() + this.url;
	}
}
